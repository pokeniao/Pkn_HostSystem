<Window x:Class="Pkn_HostSystem.Views.Windows.VerifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pkn_HostSystem.Views.Windows"
        xmlns:page="clr-namespace:Pkn_HostSystem.Models.Page"
        mc:Ignorable="d"
        Title="" Height="350" Width="600"
        d:DataContext="{d:DesignInstance page:OnStartupModel}"
        WindowStartupLocation="CenterScreen"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="机器码" Margin="10 2" />
            <TextBox IsReadOnly="True" Text="{Binding MachineCode}"
                     BorderThickness="0"
                     Margin="5 0"
                     Style="{x:Null}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="填入在此" Margin="10 2"  />
            <TextBox Grid.Row="1" 
                     Text="{Binding License ,UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Margin="5 0"
                     Style="{x:Null}"
                     />
        </Grid>
        <Button Content="确认" Grid.Row="2" 
                Click="ButtonBase_OnClick" 
                HorizontalAlignment="Center"
                Margin="0 10"
                Width="120"/>
    </Grid>
</Window>
