<ui:FluentWindow x:Class="Pkn_HostSystem.Views.Windows.AddProjectWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 WindowStartupLocation="CenterScreen"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
   
                 d:DesignHeight="714.5" d:DesignWidth="1000"
                 Title="AddProjectWindow" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <Border VerticalAlignment="Center" Height="40" MouseDown="Border_MouseDown">
                <TextBlock Text="添加项目" FontWeight="Black" FontSize="16" Margin="30,9" />
            </Border>
        </Grid>
        <TextBox Grid.Row="1" Text="{Binding ProjectName , UpdateSourceTrigger=PropertyChanged}"/>

        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="确认" Click="ButtonBase_OnClick2" />
                <Button Content="取消" Margin="10 0" Click="ButtonBase_OnClick" />
            </StackPanel>
        </Grid>

        <ui:SnackbarPresenter Name="SnackbarPresenter" />
    </Grid>
</ui:FluentWindow>